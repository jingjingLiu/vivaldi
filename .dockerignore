# Git 与本地编辑器文件不需要进入镜像上下文。
.git
.DS_Store
.vscode

# 依赖和构建产物由 Docker 构建流程重新生成。
node_modules
server/node_modules
dist
server/dist

# 环境变量文件包含敏感信息，只在 Zeabur 环境变量中配置。
.env
.env.local
server/.env

# 日志、覆盖率和上传目录不参与镜像构建。
*.log
coverage
server/coverage
server/uploads
